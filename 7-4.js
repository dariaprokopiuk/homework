//Дано некоторое число:`12345` Найдите сумму цифр этого числа.
let chislo = 12345
let arr = String(chislo) // превращаем число в строку
.split('') // разделяем массив на элементы
.map(Number) // превращаем строку в числа
.reduce((a,b)=> a + b, 0) // складываем числа
console.log(arr)